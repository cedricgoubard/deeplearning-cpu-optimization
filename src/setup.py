from setuptools import setup, find_packages


setup(
    name='dlcpu',
    version='0.0.1',
    description='Tools to optimize neural networks inference time on CPU and GPU',
    author='Arnaud Hureaux, Cédric Goubard',
    packages=find_packages()
)
